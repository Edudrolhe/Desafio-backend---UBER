spring:
  cloud:
    aws:
      region:
        static: us-east-1
      credentials:
        access-key: ${AWS_ACCESS_KEY_ID}
        secret-key: ${AWS_SECRET_ACCESS_KEY}

aws:
  sns:
    topic:
      arn: arn:aws:sns:us-east-1:123456789012:produto-notifications